apiVersion: syncit/v1
kind: Bundle
metadata:
  name: prometheus
  version: latest
spec:
  targets:
    distro: ubuntu
    codename: noble
    arch: amd64
  tasks:
    - name: Install Prometheus
      plugin: apt
      base_installroot: /tmp/debootstrap-root
      packages:
        - prometheus
