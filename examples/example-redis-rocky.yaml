apiVersion: syncit/v1
kind: Bundle
metadata:
  name: redis
  version: 7.2.4
spec:
  targets:
    distro: rocky
    codename: "9"
    arch: amd64
  tasks:
    - name: Install Redis
      plugin: dnf
      releasever: 9
      base_installroot: /tmp/rocky-root
      packages:
        - redis
