apiVersion: syncit/v1
kind: Bundle
metadata:
  name: redis
  version: 7.2.4
spec:
  targets:
    distro: ubuntu
    codename: noble
    arch: amd64
  tasks:
    - name: Install Redis
      plugin: apt
      base_installroot: /tmp/debootstrap-root
      packages:
        - redis-server
